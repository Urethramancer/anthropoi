version: '3'
services:
    app:
        image: acctsrv:latest
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '127.0.0.1:9002:8000'
        environment:
            - DB_HOST=/var/run/postgresql
            - DB_PASSWORD=FoMdi8n2qfs55V
        volumes:
            - /var/run/postgresql/:/var/run/postgresql/
